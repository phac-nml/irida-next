# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

attachment1:
  metadata: { "compression": "none", "format": "fastq" }
  attachable: sample1 (Sample)

attachment2:
  metadata: { "compression": "none", "format": "fastq" }
  attachable: sample1 (Sample)

attachment3:
  metadata: { "compression": "none", "format": "fastq" }
  attachable: sample37 (Sample)

attachment4:
  metadata: { "compression": "none", "format": "fastq" }
  attachable: sample37 (Sample)

attachmentA:
  metadata: { "compression": "none", "format": "fastq" }
  attachable: sampleA (Sample)

attachmentB:
  metadata: { "compression": "none", "format": "fastq" }
  attachable: sampleA (Sample)

attachmentC:
  metadata: { "compression": "none", "format": "fastq" }
  attachable: sampleA (Sample)

attachmentPEFWD1:
  metadata:
    {
      "type": "pe",
      "format": "fastq",
      "direction": "forward",
      "compression": "none",
      "associated_attachment_id": <%= ActiveRecord::FixtureSet.identify(:attachmentPEREV1) %>,
    }
  attachable: sampleB (Sample)

attachmentPEREV1:
  metadata:
    {
      "type": "pe",
      "format": "fastq",
      "direction": "reverse",
      "compression": "none",
      "associated_attachment_id": <%= ActiveRecord::FixtureSet.identify(:attachmentPEFWD1) %>,
    }
  attachable: sampleB (Sample)

attachmentPEFWD2:
  metadata:
    {
      "type": "pe",
      "format": "fastq",
      "direction": "forward",
      "compression": "none",
      "associated_attachment_id": <%= ActiveRecord::FixtureSet.identify(:attachmentPEREV2) %>,
    }
  attachable: sampleB (Sample)

attachmentPEREV2:
  metadata:
    {
      "type": "pe",
      "format": "fastq",
      "direction": "reverse",
      "compression": "none",
      "associated_attachment_id": <%= ActiveRecord::FixtureSet.identify(:attachmentPEFWD2) %>,
    }
  attachable: sampleB (Sample)

attachmentPEFWD3:
  metadata:
    {
      "type": "pe",
      "format": "fastq",
      "direction": "forward",
      "compression": "none",
      "associated_attachment_id": <%= ActiveRecord::FixtureSet.identify(:attachmentPEREV3) %>,
    }
  attachable: sampleB (Sample)

attachmentPEREV3:
  metadata:
    {
      "type": "pe",
      "format": "fastq",
      "direction": "reverse",
      "compression": "none",
      "associated_attachment_id": <%= ActiveRecord::FixtureSet.identify(:attachmentPEFWD3) %>,
    }
  attachable: sampleB (Sample)

attachmentPEFWD4:
  metadata:
    {
      "type": "illumina_pe",
      "format": "fastq",
      "direction": "forward",
      "compression": "none",
    }
  attachable: sampleC (Sample)

attachmentPEREV4:
  metadata:
    {
      "type": "illumina_pe",
      "format": "fastq",
      "direction": "reverse",
      "compression": "none",
    }
  attachable: sampleC (Sample)

attachmentPEFWD5:
  metadata:
    {
      "type": "illumina_pe",
      "format": "fastq",
      "direction": "forward",
      "compression": "none",
    }
  attachable: sampleC (Sample)

attachmentPEREV5:
  metadata:
    {
      "type": "illumina_pe",
      "format": "fastq",
      "direction": "reverse",
      "compression": "none",
    }
  attachable: sampleC (Sample)

attachmentD:
  metadata: { "compression": "none", "format": "fastq" }
  attachable: sampleB (Sample)

attachmentE:
  metadata: { "compression": "gzip", "format": "fastq" }
  attachable: sampleB (Sample)

attachmentF:
  metadata: { "compression": "gzip", "format": "fastq" }
  attachable: sampleB (Sample)

attachmentG:
  metadata: { "compression": "none", "format": "fastq" }
  attachable: sampleC (Sample)

attachmentH:
  metadata: { "compression": "none", "format": "fastq" }
  attachable: sampleC (Sample)

attachmentPEFWD6:
  metadata:
    {
      "type": "illumina_pe",
      "format": "fastq",
      "direction": "forward",
      "compression": "none",
    }
  attachable: sampleC (Sample)

attachmentPEREV6:
  metadata:
    {
      "type": "illumina_pe",
      "format": "fastq",
      "direction": "reverse",
      "compression": "none",
    }
  attachable: sampleC (Sample)

attachmentPEFWD7:
  metadata:
    {
      "type": "illumina_pe",
      "format": "fastq",
      "direction": "forward",
      "compression": "none",
    }
  attachable: sampleC (Sample)

attachmentPEREV7:
  metadata:
    {
      "type": "illumina_pe",
      "format": "fastq",
      "direction": "reverse",
      "compression": "none",
    }
  attachable: sampleC (Sample)

attachmentI:
  metadata: { "compression": "gzip", "format": "fastq" }
  attachable: sampleC (Sample)

attachmentJ:
  metadata: { "compression": "gzip", "format": "fastq" }
  attachable: sampleC (Sample)
