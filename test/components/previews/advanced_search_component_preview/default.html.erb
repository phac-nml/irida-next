<%= form_with model: search, scope: :q, url: "#", data: { controller: "filters" } do |form| %>
  <%= render AdvancedSearchComponent.new(
    search:,
    form:,
    sample_fields:,
    metadata_fields:,
  ) %>
<% end %>
