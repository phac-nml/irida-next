<div class="p-6 space-y-4 max-w-md">
  <h3 id="assembly-outputs" class="text-lg font-semibold">
    Assembly Output Selection
  </h3>

  <p class="text-gray-600 text-sm">
    Choose whether to include specific assembly-derived outputs. The checkbox
    is labeled by the heading above via aria-labelledby.
  </p>

  <%= pathogen_checkbox(
    "preview_object",
    :include_gff,
    {
      aria: {
        labelledby: "assembly-outputs",
      },
      help_text: "Export annotated GFF file along with assembly contigs",
    },
    "1",
    "0",
  ) %>

  <hr class="my-4">

  <h4 id="batch-processing" class="text-md font-medium">
    Batch Processing Control
  </h4>

  <%= pathogen_checkbox(
    "preview_object",
    :enable_batch_qc,
    {
      aria: {
        labelledby: "batch-processing",
      },
      help_text: "Apply identical QC thresholds to all listed samples",
      controls: "data-table",
    },
    "1",
    "0",
  ) %>

  <div id="data-table" class="mt-2 p-3 border rounded bg-gray-50">
    <p class="text-sm text-gray-600">
      This simulates a list of samples that would have standardized QC
      thresholds applied when batch processing is enabled.
    </p>
  </div>
</div>
