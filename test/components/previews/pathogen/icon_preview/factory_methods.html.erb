<div class="p-8 bg-gradient-to-br from-emerald-50 to-teal-100 dark:from-slate-900 dark:to-slate-800 min-h-screen">
  <!-- Header -->
  <div class="mb-12 text-center">
    <h1 class="text-4xl font-bold text-slate-900 dark:text-slate-100 mb-4">
      <%= render Pathogen::Icon.new("hammer", color: :primary, size: :xl, class: "inline mr-3") %>
      Factory Methods & Enhanced Features
    </h1>
    <p class="text-lg text-slate-600 dark:text-slate-400 max-w-3xl mx-auto">
      Convenient shortcuts and powerful new features for better developer experience.
    </p>
  </div>

  <!-- Color Factory Methods -->
  <div class="mb-12">
    <h2 class="text-2xl font-semibold text-slate-900 dark:text-slate-100 mb-6 flex items-center">
      <%= render Pathogen::Icon.new("paint-brush", color: :primary, class: "mr-3") %>
      Color Factory Methods
    </h2>
    <p class="text-slate-600 dark:text-slate-400 mb-6">Quick shortcuts for common color patterns.</p>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% [
        { method: "success", icon: "check-circle", color: :success, desc: "Perfect for confirmations and positive actions" },
        { method: "danger", icon: "warning", color: :danger, desc: "Ideal for errors, warnings, and destructive actions" },
        { method: "warning", icon: "warning-circle", color: :warning, desc: "Great for caution messages and alerts" },
        { method: "primary", icon: "star", color: :primary, desc: "Use for brand elements and CTAs" },
        { method: "subdued", icon: "info", color: :subdued, desc: "Perfect for secondary information" }
      ].each do |item| %>
        <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-700">
          <div class="flex items-center justify-center h-16 bg-slate-50 dark:bg-slate-700 rounded-lg mb-4">
            <%= render Pathogen::Icon.send(item[:method], item[:icon], size: :lg) %>
          </div>
          <h3 class="font-semibold text-slate-900 dark:text-slate-100 mb-2">
            Icon.<%= item[:method] %>()
          </h3>
          <p class="text-sm text-slate-600 dark:text-slate-400 mb-3"><%= item[:desc] %></p>
          <code class="text-xs bg-slate-100 dark:bg-slate-700 px-2 py-1 rounded text-slate-700 dark:text-slate-300 block">
            Pathogen::Icon.<%= item[:method] %>("<%= item[:icon] %>")
          </code>
        </div>
      <% end %>
    </div>

    <!-- Factory Methods in Action -->
    <div class="mt-8 bg-white dark:bg-slate-800 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-700">
      <h3 class="font-semibold text-slate-900 dark:text-slate-100 mb-4">Factory Methods in Action</h3>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <h4 class="font-medium text-slate-900 dark:text-slate-100 mb-3">Status Messages</h4>
          <div class="space-y-3">
            <div class="flex items-center gap-3 p-3 bg-green-50 dark:bg-green-900/20 rounded-lg">
              <%= render Pathogen::Icon.success("check-circle") %>
              <span class="text-green-800 dark:text-green-200">Payment processed successfully</span>
            </div>
            <div class="flex items-center gap-3 p-3 bg-yellow-50 dark:bg-yellow-900/20 rounded-lg">
              <%= render Pathogen::Icon.warning("warning") %>
              <span class="text-yellow-800 dark:text-yellow-200">Account verification pending</span>
            </div>
            <div class="flex items-center gap-3 p-3 bg-red-50 dark:bg-red-900/20 rounded-lg">
              <%= render Pathogen::Icon.danger("x-circle") %>
              <span class="text-red-800 dark:text-red-200">Unable to connect to server</span>
            </div>
          </div>
        </div>
        <div>
          <h4 class="font-medium text-slate-900 dark:text-slate-100 mb-3">Interface Elements</h4>
          <div class="space-y-3">
            <button class="flex items-center gap-2 px-4 py-2 bg-primary-600 text-white rounded-lg w-full">
              <%= render Pathogen::Icon.primary("plus", size: :sm, class: "text-white") %>
              Create New Project
            </button>
            <div class="flex items-center gap-3 text-slate-600 dark:text-slate-400 text-sm">
              <%= render Pathogen::Icon.subdued("clock") %>
              <span>Last updated 5 minutes ago</span>
            </div>
            <div class="flex items-center justify-between p-3 border border-slate-200 dark:border-slate-700 rounded-lg">
              <span class="text-slate-900 dark:text-slate-100">Delete Account</span>
              <%= render Pathogen::Icon.danger("trash", size: :sm) %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Size Factory Methods -->
  <div class="mb-12">
    <h2 class="text-2xl font-semibold text-slate-900 dark:text-slate-100 mb-6 flex items-center">
      <%= render Pathogen::Icon.new("arrows-out", color: :primary, class: "mr-3") %>
      Size Factory Methods
    </h2>
    <p class="text-slate-600 dark:text-slate-400 mb-6">Quick shortcuts for common size patterns.</p>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-700 text-center">
        <div class="flex items-center justify-center h-16 bg-slate-50 dark:bg-slate-700 rounded-lg mb-4">
          <%= render Pathogen::Icon.small("heart", color: :danger) %>
        </div>
        <h3 class="font-semibold text-slate-900 dark:text-slate-100 mb-2">Icon.small()</h3>
        <p class="text-sm text-slate-600 dark:text-slate-400 mb-3">Perfect for inline use in text</p>
        <code class="text-xs bg-slate-100 dark:bg-slate-700 px-2 py-1 rounded text-slate-700 dark:text-slate-300">
          Icon.small("heart")
        </code>
      </div>

      <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-700 text-center">
        <div class="flex items-center justify-center h-16 bg-slate-50 dark:bg-slate-700 rounded-lg mb-4">
          <%= render Pathogen::Icon.large("star", color: :primary) %>
        </div>
        <h3 class="font-semibold text-slate-900 dark:text-slate-100 mb-2">Icon.large()</h3>
        <p class="text-sm text-slate-600 dark:text-slate-400 mb-3">Great for feature highlights</p>
        <code class="text-xs bg-slate-100 dark:bg-slate-700 px-2 py-1 rounded text-slate-700 dark:text-slate-300">
          Icon.large("star")
        </code>
      </div>

      <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-700 text-center">
        <div class="flex items-center justify-center h-16 bg-slate-50 dark:bg-slate-700 rounded-lg mb-4">
          <%= render Pathogen::Icon.extra_large("rocket-launch", color: :success) %>
        </div>
        <h3 class="font-semibold text-slate-900 dark:text-slate-100 mb-2">Icon.extra_large()</h3>
        <p class="text-sm text-slate-600 dark:text-slate-400 mb-3">Ideal for hero sections</p>
        <code class="text-xs bg-slate-100 dark:bg-slate-700 px-2 py-1 rounded text-slate-700 dark:text-slate-300">
          Icon.extra_large("rocket")
        </code>
      </div>
    </div>
  </div>

  <!-- Accessibility Features -->
  <div class="mb-12">
    <h2 class="text-2xl font-semibold text-slate-900 dark:text-slate-100 mb-6 flex items-center">
      <%= render Pathogen::Icon.new("eye", color: :primary, class: "mr-3") %>
      Accessibility Features
    </h2>
    <p class="text-slate-600 dark:text-slate-400 mb-6">Built-in accessibility with semantic and decorative icon handling.</p>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <!-- Semantic Icons -->
      <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-700">
        <h3 class="font-semibold text-slate-900 dark:text-slate-100 mb-4 flex items-center">
          <%= render Pathogen::Icon.primary("info", "aria-label": "Information", size: :sm, class: "mr-2") %>
          Semantic Icons
        </h3>
        <p class="text-sm text-slate-600 dark:text-slate-400 mb-4">Icons that convey meaning and should be announced by screen readers.</p>

        <div class="space-y-4">
          <div class="p-3 bg-blue-50 dark:bg-blue-900/20 rounded-lg border-l-4 border-blue-400">
            <%= render Pathogen::Icon.primary("info", "aria-label": "Important information", class: "float-left mr-2 mt-0.5") %>
            <p class="text-blue-800 dark:text-blue-200 text-sm">This icon conveys important information and includes an aria-label for screen readers.</p>
          </div>

          <code class="block text-xs bg-slate-100 dark:bg-slate-700 p-3 rounded text-slate-700 dark:text-slate-300">
            &lt;%= render Pathogen::Icon.new("info", "aria-label": "Important information") %&gt;
          </code>

          <div class="text-xs text-slate-500 dark:text-slate-400">
            <strong>Features:</strong> aria-hidden="false", role="img", requires aria-label
          </div>
        </div>
      </div>

      <!-- Decorative Icons -->
      <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-700">
        <h3 class="font-semibold text-slate-900 dark:text-slate-100 mb-4 flex items-center">
          <%= render Pathogen::Icon.primary("sparkle", size: :sm, class: "mr-2", "aria-hidden": true) %>
          Decorative Icons
        </h3>
        <p class="text-sm text-slate-600 dark:text-slate-400 mb-4">Icons that are purely visual and should be hidden from screen readers.</p>

        <div class="space-y-4">
          <div class="p-3 bg-slate-50 dark:bg-slate-700 rounded-lg">
            <h4 class="font-medium text-slate-900 dark:text-slate-100 flex items-center">
              <%= render Pathogen::Icon.primary("sparkle", size: :sm, class: "mr-2", "aria-hidden": true) %>
              Beautiful Design
            </h4>
            <p class="text-slate-600 dark:text-slate-400 text-sm mt-1">The sparkle icon is decorative and enhances the visual appeal.</p>
          </div>

          <code class="block text-xs bg-slate-100 dark:bg-slate-700 p-3 rounded text-slate-700 dark:text-slate-300">
            &lt;%= render Pathogen::Icon.new("sparkle", "aria-hidden": true) %&gt;
          </code>

          <div class="text-xs text-slate-500 dark:text-slate-400">
            <strong>Features:</strong> aria-hidden="true" (hidden from screen readers)
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Error Handling & Fallbacks -->
  <div>
    <h2 class="text-2xl font-semibold text-slate-900 dark:text-slate-100 mb-6 flex items-center">
      <%= render Pathogen::Icon.new("shield-check", color: :primary, class: "mr-3") %>
      Error Handling & Fallbacks
    </h2>
    <p class="text-slate-600 dark:text-slate-400 mb-6">Robust error handling with helpful developer feedback and graceful fallbacks.</p>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <!-- Development Mode -->
      <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-700">
        <h3 class="font-semibold text-slate-900 dark:text-slate-100 mb-4">Development Mode</h3>
        <p class="text-sm text-slate-600 dark:text-slate-400 mb-4">Invalid icons show helpful error messages with suggestions.</p>

        <div class="space-y-3">
          <div class="p-3 bg-red-50 dark:bg-red-900/20 rounded-lg border border-red-200 dark:border-red-800">
            <span class="text-red-500 text-xs font-mono border border-red-300 rounded px-2 py-1 bg-red-50">
              ⚠️ Icon 'invalid-icon-name' not found (Suggestions: check, check-circle, check-badge)
            </span>
          </div>

          <code class="block text-xs bg-slate-100 dark:bg-slate-700 p-3 rounded text-slate-700 dark:text-slate-300">
            &lt;%= render Pathogen::Icon.new("invalid-icon-name") %&gt;
          </code>
        </div>

        <div class="mt-4 text-xs text-slate-500 dark:text-slate-400">
          <strong>Features:</strong> Smart suggestions, debug CSS classes, detailed error info
        </div>
      </div>

      <!-- Production Mode -->
      <div class="bg-white dark:bg-slate-800 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-700">
        <h3 class="font-semibold text-slate-900 dark:text-slate-100 mb-4">Production Mode</h3>
        <p class="text-sm text-slate-600 dark:text-slate-400 mb-4">Graceful fallbacks prevent layout breakage.</p>

        <div class="space-y-3">
          <div class="p-3 bg-slate-50 dark:bg-slate-700 rounded-lg">
            <div class="flex items-center gap-3">
              <%= render Pathogen::Icon.new("question", color: :subdued) %>
              <span class="text-slate-600 dark:text-slate-400">Fallback icon when primary fails</span>
            </div>
          </div>

          <div class="text-sm text-slate-600 dark:text-slate-400">
            If no fallback works, renders nothing (graceful degradation)
          </div>
        </div>

        <div class="mt-4 text-xs text-slate-500 dark:text-slate-400">
          <strong>Features:</strong> Silent failures, fallback icons, layout preservation
        </div>
      </div>
    </div>

    <!-- Parameter Validation -->
    <div class="mt-6 bg-white dark:bg-slate-800 rounded-xl p-6 shadow-sm border border-slate-200 dark:border-slate-700">
      <h3 class="font-semibold text-slate-900 dark:text-slate-100 mb-4">Parameter Validation</h3>
      <p class="text-sm text-slate-600 dark:text-slate-400 mb-4">Helpful warnings for invalid parameters with automatic fallbacks.</p>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <h4 class="font-medium text-slate-900 dark:text-slate-100 mb-2">Invalid Color</h4>
          <div class="text-xs text-orange-600 dark:text-orange-400 mb-2 font-mono bg-orange-50 dark:bg-orange-900/20 p-2 rounded">
            [Pathogen::Icon] Invalid color 'purple', falling back to :default. Valid colors: default, primary, success, warning, danger, subdued
          </div>
          <code class="text-xs bg-slate-100 dark:bg-slate-700 p-2 rounded block">
            Icon.new("star", color: :purple)
          </code>
        </div>
        <div>
          <h4 class="font-medium text-slate-900 dark:text-slate-100 mb-2">Invalid Size</h4>
          <div class="text-xs text-orange-600 dark:text-orange-400 mb-2 font-mono bg-orange-50 dark:bg-orange-900/20 p-2 rounded">
            [Pathogen::Icon] Invalid size 'huge', falling back to :md. Valid sizes: sm, md, lg, xl
          </div>
          <code class="text-xs bg-slate-100 dark:bg-slate-700 p-2 rounded block">
            Icon.new("star", size: :huge)
          </code>
        </div>
      </div>
    </div>
  </div>
</div>