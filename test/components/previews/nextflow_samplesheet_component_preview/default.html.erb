<%= render Nextflow::SamplesheetComponent.new(
  schema:
    JSON.parse(
      File.read(
        Rails.root.join("test", "fixtures", "files", "nextflow", schema_file)
      )
    ),
  samples:,
  fields: %[metadata1, metadata2, metadata3],
  namespace_id: "SDFSDFSDF"
) %>
