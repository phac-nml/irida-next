<%# This should be loaded once into the layout %>
<%= render ConfirmationComponent.new %>
<%# ------------------------------------------ %>

<div class="hidden custom-content">
  <%= viral_alert(
    message: "You are about to delete this project.  This cannot be undone.",
    type: :alert
  ) %>
  <p class="text-base text-gray-900 dark:text-white">Enter the following to confirm:</p>
  <p class="text-gray-500 dark:text-gray-400">
    <kbd
      class="
        px-2
        py-1.5
        text-xs
        font-semibold
        text-gray-800
        bg-gray-100
        border
        border-gray-200
        rounded-lg
        dark:bg-gray-600
        dark:text-gray-100
        dark:border-gray-500
      "
    >
      Project X
    </kbd>
  </p>
</div>

<%= button_to "Delete project",
"#",
data: {
  turbo_confirm: "",
  turbo_content: ".custom-content",
  confirm_value: "Project X"
},
class: "button button--state-destructive button--size-default" %>
