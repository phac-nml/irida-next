<div class="flex gap-1">
  <%= p_button(scheme: :default) do %>
    Default
  <% end %>

  <%= p_button(scheme: :primary) do %>
    Primary
  <% end %>
  <%= p_button(scheme: :danger) do %>
    Danger
  <% end %>
  <%= p_button(scheme: :ghost) do %>
    Ghost
  <% end %>

  <%# 💡 Added unstyled button example with custom classes %>
  <%= p_button(scheme: :unstyled, class: 'bg-purple-500 text-white p-2 rounded-md hover:bg-purple-600') do %>
    Unstyled (Custom)
  <% end %>
</div>
