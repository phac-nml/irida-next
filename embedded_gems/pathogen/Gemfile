# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

gem 'rails', '>= 8.1.1' # Required for security patches (nokogiri >= 1.18.9, rack >= 3.1.18, activerecord >= 8.0.2.1, uri >= 1.0.4)

# development dependencies
group :development do
  gem 'annotate'
  gem 'rubocop'
  gem 'rubocop-performance'
  gem 'rubocop-rails'
  gem 'rubocop-rails-accessibility', '~> 1.0'
end
