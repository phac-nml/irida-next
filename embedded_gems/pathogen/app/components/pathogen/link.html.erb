<%= render Pathogen::ConditionalWrapper.new(condition: tooltip?, tag: :div, 'data-controller': 'viral--tooltip') do %>
  <%= render(Pathogen::BaseComponent.new(**@link_system_arguments.merge(tooltip? ? @tooltip_system_arguments : {}))) do %>
    <%= content %>
  <% end %>

  <% if tooltip.present? %>
    <%= render tooltip %>
  <% end %>
<% end %>
