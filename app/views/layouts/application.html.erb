<!DOCTYPE html>
<html class="light" lang="<%= locale %>">
  <%= render "layouts/partials/head" %>
  <body class="bg-white dark:bg-gray-900" data-controller="dark-mode">
    <%= render LayoutComponent.new(user: current_user) do |layout| %>
    <% layout.sidebar(label: t(:"general.default_sidebar.title"), icon_name: "inbox_stack") do |navigation| %>
        <%= navigation.with_section do |section| %>
          <%= render section.with_item(
            icon: "rectangle_stack",
            label: t(:"general.default_sidebar.projects"),
            url: dashboard_projects_path
          ) %>
          <%= render section.with_item(
            icon: "squares_2x2",
            label: t(:"general.default_sidebar.groups"),
            url: groups_path
          ) %>
        <% end %>
        <% layout.body do %>
          <%= yield %>
        <% end %>
      <% end %>
    <% end %>
  </body>
</html>
