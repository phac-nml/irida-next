<!DOCTYPE html>
<html class="light" lang="<%= locale %>">
<head>
  <title>Irida</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
</head>
<body class="bg-gray-50 dark:bg-gray-900">
<div class="grid h-screen place-items-center">
  <section class="w-full bg-white rounded-lg shadow dark:border md:mt-0 sm:max-w-md xl:p-0 dark:bg-gray-800 dark:border-gray-700">
    <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
      <div class="flex flex-col items-center justify-between">
        <%= render Viral::IconComponent.new(name: "beaker", classes: "w-20 h-20 text-green-500 rotate-12") %>
        <h1 class="text-xl font-bold leading-tight tracking-tight text-center text-gray-900 md:text-2xl dark:text-white">IRIDA
          Next</h1>
      </div>
      <% flash.each do |type, msg| %>
        <%= viral_alert(type: type, message: msg) %>
      <% end %>
      <%= yield %>
    </div>
  </section>
</body>
</html>
