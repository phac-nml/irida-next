<%= render partial: "shared/metadata_templates/header",
locals: {
  title: t(:".title"),
  subtitle: t(:".subtitle"),
  new_path:
    new_namespace_project_metadata_template_path(
      @namespace.parent,
      @namespace.project,
      tab: @tab,
    ),
  namespace: @namespace,
} %>

<%= render partial: "shared/metadata_templates/table" %>
