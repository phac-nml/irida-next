<% unless @namespace_group_link.errors.any? %>
  <%= turbo_stream.append "flashes" do %>
    <%= viral_flash(type:, data: message) %>
  <% end %>
<% end %>

<%= turbo_stream.update "edit_dialog",
                      partial: "edit_modal",
                      locals: {
                        open: @namespace_group_link.errors.any?,
                        namespace_group_link: @namespace_group_link,
                        namespace: @namespace
                      } %>

<turbo-stream action="refresh"></turbo-stream>
