<%= turbo_stream.update "edit_dialog",
                    partial: "edit_modal",
                    locals: {
                      open: true,
                      access_levels: @access_levels,
                      member: @member,
                      namespace: @namespace,
                    } %>
