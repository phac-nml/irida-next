<%= turbo_stream.update "automated_workflow_execution_modal" do %>
  <%= render Viral::AlertComponent.new(
    title: type == 'success' ? t("common.alerts.success") : t("common.alerts.error"),
    description: message,
    type: type
  ) %>
<% end %>

<%= turbo_stream.update "automated_workflow_executions_table", src: namespace_project_automated_workflow_executions_path(format: :turbo_stream) %>
