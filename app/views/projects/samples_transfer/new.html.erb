<%= render Viral::ModalComponent.new(title: t(".title")) do |modal| %>
  <% modal.with_body do %>
    <%= form_with(url: namespace_project_samples_transfer_index_path, method: :post, data: {
              turbo: false,
              controller: "form"
            }) do |form| %>
      <%= form.hidden_field "sample_ids", multiple: true, data: { form_target: "field" } %>
      <div class="form-field">
        <%= form.label :new_project_id %>
        <%= form.collection_select(:new_project_id, @projects, :id, :full_path) %>
        <%= render "shared/form/field_errors",
        errors: @project.errors.full_messages_for(:base) %>
      </div>
      <div class="my-2">
        <%= form.submit t(".submit_button"), class: "btn btn-primary" %>
      </div>
    <% end %>
  <% end %>
<% end %>
