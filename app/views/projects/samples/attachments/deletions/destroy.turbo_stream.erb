<%= turbo_stream.update(
  "sample_files_modal",
  partial: "modal",
  locals: {
    open: false
  }
) %>

<% if !message.nil? %>
  <%= turbo_stream.append "flashes" do %>
    <%= viral_flash(type:, data: message) %>
  <% end %>
<% end %>

<% unless not_deleted_atts.nil? %>
  <% not_deleted_atts.each do |attachment| %>
    <%= turbo_stream.append "flashes" do %>
      <%= viral_flash(
        type: :error,
        data:
          t(
            ".error",
            filename: attachment.file.filename,
            errors: attachment.errors.full_messages.first
          )
      ) %>
    <% end %>
  <% end %>
<% end %>

<%= turbo_stream.update(
  "attachments",
  partial: "projects/samples/attachments/table",
  locals: {
    attachments: @sample.attachments
  }
) %>
