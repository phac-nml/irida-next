<%= render partial: "shared/samples/transfers/new",
locals: {
  list_samples_path:
    list_namespace_project_samples_path(
      @project.namespace.parent,
      @project,
      list_class: "sample",
    ),
  transfer_path:
    namespace_project_samples_transfer_path(
      @project.namespace.parent,
      @project,
    ),
  broadcast_target: @broadcast_target,
} %>
