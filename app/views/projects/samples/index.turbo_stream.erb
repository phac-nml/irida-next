<%= turbo_stream.update "project_samples_list",
                    partial: "table",
                    locals: {
                      project: @project,
                      samples: @samples
                    } %>

<%= turbo_stream.update "project_samples_pagination",
                    partial: "pagination",
                    locals: {
                      project: @project,
                      pagy: @pagy
                    } %>

<%= turbo_stream.update "project_samples_sort_links",
                    partial: "sort_links",
                    locals: {
                      ransack_obj: @q
                    } %>

<%= turbo_stream.update "project_samples_hidden_search_value",
                    partial: "hidden_search_value",
                    locals: {
                      ransack_obj: @q
                    } %>
