<%= turbo_stream.update "table-filter",
                    partial: "table_filter",
                    locals: {
                      query: @query,
                    } %>

<turbo-stream action="refresh"></turbo-stream>
