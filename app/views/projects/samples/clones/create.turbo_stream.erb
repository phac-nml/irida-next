<%= turbo_stream.update "samples_dialog",
                    partial: "dialog",
                    locals: {
                      open: true,
                      closable: false,
                    } %>

<%= turbo_stream.update "clone_samples_dialog_content" do %>
  <%= render Viral::ProgressBarComponent.new(items_to_complete: @samples_count) %>
<% end %>
