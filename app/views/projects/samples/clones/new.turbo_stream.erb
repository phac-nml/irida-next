<%= turbo_stream.update "samples_dialog",
                    partial: "dialog",
                    locals: {
                      open: true,
                      closable: true,
                    } %>
