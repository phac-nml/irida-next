<%= turbo_stream.update "bot_modal",
                    partial: "bot_modal",
                    locals: {
                      open: true,
                      new_bot_account: @new_bot_account,
                      bot_params: nil,
                    } %>
