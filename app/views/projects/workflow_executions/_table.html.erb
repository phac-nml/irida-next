<%# locals: (namespace:, pagy:, q:, workflow_executions:) -%>
<%= render WorkflowExecutions::TableComponent.new(
  workflow_executions,
  pagy,
  q,
  namespace: namespace,
  row_actions: {
    cancel: allowed_to?(:update?, namespace),
    destroy: allowed_to?(:update?, namespace),
  },
  empty: {
    title: t(:".empty.title"),
    description: t(:".empty.description"),
  },
) %>
