<%= turbo_stream.update "edit_dialog",
                    partial: "edit_modal",
                    locals: {
                      open: true,
                      access_levels: @access_levels,
                      namespace_group_link: @namespace_group_link,
                      namespace: @namespace,
                      tab: @tab
                    } %>
