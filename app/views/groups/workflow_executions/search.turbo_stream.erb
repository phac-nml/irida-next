<%= turbo_stream.update "table-filter", partial: "table_filter" %>

<% if @query.valid? %>
  <%= turbo_stream.append "workflow-executions-table" do %>
    <%= render SpinnerComponent.new(message: t("groups.workflow_executions.index.loading")) %>
  <% end %>

  <turbo-stream action="refresh"></turbo-stream>
<% end %>
