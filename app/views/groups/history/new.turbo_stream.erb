<%= turbo_stream.update "history_modal",
                    partial: "group_history_modal",
                    locals: {
                      open: true,
                      log_data: @log_data,
                    } %>
