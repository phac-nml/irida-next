<%= turbo_stream.update "bot_modal",
                    partial: "bot_modal",
                    locals: {
                      open: true,
                      access_levels: @access_levels,
                      bot_account: @bot_account,
                    } %>
