<%= turbo_stream.append "flashes" do %>
  <%= viral_flash(type:, data: message) %>
<% end %>
<% if @bot_account.deleted? %>
  <%= turbo_stream.remove "access-token-section" %>
  <turbo-stream action="refresh"></turbo-stream>
<% end %>
