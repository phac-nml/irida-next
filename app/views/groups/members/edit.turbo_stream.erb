<%= turbo_stream.update "edit_dialog",
                    partial: "edit_modal",
                    locals: {
                      open: true,
                      member: @member,
                      namespace: @namespace,
                    } %>
