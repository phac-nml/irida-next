<%= turbo_stream.replace dom_id(@group) do %>
  <%= render GroupsList::GroupRow::WithChildrenComponent.new(
    group: @group,
    children: @children,
    collapsed: @collapsed,
    path: "group_path"
  ) %>
<% end %>
