<%= turbo_stream.update "group_samples_table",
                    partial: "table",
                    locals: {
                      group: @group,
                      pagy: @pagy
                      samples: @samples,
                    } %>

  <%= turbo_stream.update "group_samples_hidden_values" do %>
    <%= render Ransack::HiddenSortFieldComponent.new(@q) %>
  <% end %>
