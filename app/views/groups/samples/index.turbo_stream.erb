<%= turbo_stream.update "group_samples_list",
                    partial: "table",
                    locals: {
                      group: @group,
                      samples: @samples
                    } %>

<%= turbo_stream.update "group_samples_pagination",
                    partial: "pagination",
                    locals: {
                      group: @group,
                      pagy: @pagy
                    } %>
