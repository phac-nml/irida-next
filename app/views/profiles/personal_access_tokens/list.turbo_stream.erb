<%= turbo_stream.update "personal-access-tokens-listing",
                      partial: "table",
                      locals: {
                        access_tokens: @personal_access_tokens,
                        title: @empty_title,
                        description: @empty_description,
                      } %>
