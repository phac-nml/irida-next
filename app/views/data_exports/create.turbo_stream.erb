<% if keep_dialog_open %>
  <%= turbo_stream.update "data-export-dialog-errors" do %>
    <%= viral_alert(type:, message:, classes: "mb-2") %>
  <% end %>
<% else %>
  <% if export_type == 'analysis' %>
    <%= turbo_stream.update "export_dialog", "" %>
  <% else %>
    <%= turbo_stream.update "samples_dialog", "" %>
  <% end %>
  <%= turbo_stream.append "flashes" do %>
    <%= viral_flash(type:, data: message) %>
  <% end %>
<% end %>
