<%= turbo_stream.update "samplesheet",
                    partial: "samplesheet_samples",
                    locals: {
                      namespace_id: @namespace_id,
                      fields: @fields,
                      schema: @schema,
                      samples: @samples,
                      workflow: @workflow,
                    } %>

<%= turbo_stream.prepend "workflow_execution_message" do
  viral_alert(
    type: "success",
    message: "updated!",
    aria: {
      live: "assertive",
    },
    classes: "mb-2",
  )
end %>
