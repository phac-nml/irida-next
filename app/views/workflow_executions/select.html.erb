<%= turbo_frame_tag "selected" do %>
  <div
    data-controller="table-selection"
    data-table-selection-ids-value="<%= @workflow_executions.map { |we| we.id.to_s } %>"
    data-table-selection-selection-outlet="#workflow-executions-table"
  ></div>
<% end %>
