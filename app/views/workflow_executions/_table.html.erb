<%# locals: (pagy:, q:, workflow_executions:) -%>
<%= render WorkflowExecutions::TableComponent.new(
  workflow_executions,
  pagy,
  q,
  search_params: @search_params,
  abilities: {
    select_workflow_executions: true
  },
  row_actions: {
    cancel: true,
    destroy: true,
  },
  empty: {
    title: t(:".empty.title"),
    description: t(:".empty.description"),
  },
) %>
