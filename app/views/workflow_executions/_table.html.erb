<%# locals: (namespace: nil, pagy:, q:, workflow_executions:, has_workflow_executions:, empty_title:, empty_description:) -%>
<%= render WorkflowExecutions::TableComponent.new(
  workflow_executions,
  pagy,
  q,
  has_workflow_executions,
  namespace: namespace,
  search_params: @search_params,
  abilities: {
    select_workflow_executions: true,
  },
  row_actions: {
    cancel: @allowed_to[:cancel],
    destroy: @allowed_to[:destroy],
  },
  empty: {
    title: empty_title,
    description: empty_description,
  },
) %>
