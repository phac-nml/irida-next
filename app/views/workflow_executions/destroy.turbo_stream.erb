<%= turbo_stream.append "flashes" do %>
  <%= viral_flash(type:, data: message) %>
<% end %>

<%= turbo_stream.update "workflow_execution_dialog",
                    partial:
                      "shared/workflow_executions/destroy_confirmation_dialog",
                    locals: {
                      open: false,
                    } %>

<turbo-stream action="refresh"></turbo-stream>
