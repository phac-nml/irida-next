<%= turbo_stream.update "samples_dialog",
                    partial: "pipeline_selection_dialog",
                    locals: {
                      workflows: @workflows,
                    } %>
