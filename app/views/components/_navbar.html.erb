<nav class="fixed z-30 w-full bg-white border-b border-gray-200 dark:bg-gray-800 dark:border-gray-700"">
  <div class="px-3 lg:px-5">
    <div class="flex flex-wrap items-center justify-between mx-auto">
      <div class="flex">
        <a href="/" class="flex items-center mr-4">
          <%= render "icons/beaker", class_names: "w-12 h-12 text-green-500 rotate-12" %>
          <span class="text-xl font-semibold text-gray-700 dark:text-gray-200">IRIDA Next</span>
        </a>
        <form>
          <div class="form-field">
            <input type="search" id="first_name" placeholder="Search IRDA">
          </div>
        </form>
      </div>
      <div class="hidden w-full md:block md:w-auto" id="navbar-default">
        <ul class="flex flex-col p-4 mt-4 border border-gray-100 rounded-lg bg-gray-50 md:flex-row md:space-x-8 md:mt-0 md:text-sm md:font-medium md:border-0 md:bg-white dark:bg-gray-800 md:dark:bg-gray-900 dark:border-gray-700">
          <li>
            <button id="dropdownDefaultButton" data-dropdown-toggle="profile-dropdown" type="button" class="inline-flex items-center ">
              <%= render 'icons/user_circle', class_names: "w-6 h-6" %>
              <%= render 'icons/chevron_down', class_names: "w-4 h-4 ml-1" %>
            </button>
            <!-- Dropdown menu -->
            <div id="profile-dropdown" class="dropdown-menu">
              <div class="px-4 py-3 text-sm text-gray-900 dark:text-white">
                <div>Fred Penner</div>
                <div class="font-medium text-slate-400 truncate"><%= current_user.email %></div>
              </div>
              <ul aria-labelledby="dropdownDefaultButton">
                <li>
                  <%= link_to 'Edit profile', edit_user_registration_path %>
                </li>
                <li>
                  <%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete } %>
                </li>
              </ul>
            </div>
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>
