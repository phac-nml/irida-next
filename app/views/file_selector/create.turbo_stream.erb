<%= turbo_stream.update "file_selector_dialog",
                    partial: "file_selector/file_selector_dialog",
                    locals: {
                      open: false,
                    } %>

<turbo-stream action="replace" target=<%= "#{@attachable.id}_fastq_1"%>>
  <template>
    <%= render Nextflow::Samplesheet::FileCellComponent.new(
      @attachable,
      "fastq_1",
      @attachment_params,
      @index,
      true,
    ) %>
  </template>
</turbo-stream>
