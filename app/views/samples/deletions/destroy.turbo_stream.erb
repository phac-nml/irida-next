<%= turbo_stream.update "samples_dialog",
                    partial: @confirmation_dialog,
                    locals: {
                      open: @sample_deletion_form.errors.any?,
                      sample_deletion_form: @sample_deletion_form,
                      namespace: @namespace,
                      sample: @sample,
                    } %>
