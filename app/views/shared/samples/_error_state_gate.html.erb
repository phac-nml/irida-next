<%# locals: (errors:, message:, type: :alert) %>

<% if errors.present? %>
  <%= render partial: "shared/samples/errors",
             locals: {
               type: type,
               message: message,
               errors: errors
             } %>
<% else %>
  <%= yield %>
<% end %>
