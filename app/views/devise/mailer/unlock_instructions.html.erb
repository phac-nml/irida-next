<p><%= t(".greeting", email: @resource.email) %></p>

<p><%= t(".locked_info") %></p>

<p><%= t(".instructions") %></p>

<p><%= link_to t(".unlock_button"), unlock_url(@resource, unlock_token: @token) %></p>
