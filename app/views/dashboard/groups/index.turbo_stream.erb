<%= turbo_stream.replace dom_id(@group) do %>
  <%= render GroupList::GroupRowComponent.new(
    group: @group,
    collapse: @collapse,
    path: "dashboard_groups_path",
    path_args: {
    }
  ) %>
<% end %>
