<%= sorting_dropdown(ransack_obj) do |dropdown| %>
    <%= sorting_item(dropdown, ransack_obj, :created_at, :desc, "groups") %>
    <%= sorting_item(dropdown, ransack_obj, :created_at, :asc, "groups") %>
    <%= sorting_item(dropdown, ransack_obj, :name, :asc, "groups") %>
    <%= sorting_item(dropdown, ransack_obj, :name, :desc, "groups") %>
    <%= sorting_item(dropdown, ransack_obj, :updated_at, :desc, "groups") %>
    <%= sorting_item(dropdown, ransack_obj, :updated_at, :asc, "groups") %>
<% end %>
