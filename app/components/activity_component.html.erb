<ol class="relative border-s border-gray-200 dark:border-gray-700">
  <% activities.each do |activity| %>
    <li class="mb-10 ms-4">
      <div
        class="
          absolute
          w-3
          h-3
          bg-gray-200
          rounded-full
          mt-1.5
          -start-1.5
          border
          border-white
          dark:border-gray-900
          dark:bg-gray-700
        "
      ></div>
      <time
        class="
          mb-1
          text-sm
          font-normal
          leading-none
          text-gray-400
          dark:text-gray-500
        "
      ><%= activity.created_at %></time>
      <h3 class="text-lg font-semibold text-gray-900 dark:text-white"></h3>
      <p class="text-base font-normal text-gray-500 dark:text-gray-400">
        <%= t(
          "activity.namespaces_project_namespace.create",
          user: activity.owner.email,
          name:
            (
              if !activity.trackable.nil?
                activity.trackable.name
              else
                activity.parameters["name"]
              end
            )
        ) %>

      </p>
    </li>
  <% end %>
</ol>
