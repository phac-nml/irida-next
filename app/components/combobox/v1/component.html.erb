<%= tag.div(
  data: {
    controller: "v1--combobox",
    "v1--combobox-no-results-text-value": t("combobox_component.no_results_text"),
    "v1--combobox-single-result-text-value": t("combobox_component.single_result_text"),
    "v1--combobox-multiple-results-text-value": t("combobox_component.multiple_results_text"),
  },
) do %>
  <%= @form.hidden_field @field,
                     value: @selected_option[:value],
                     id: "#{@combobox_id}_hidden",
                     data: {
                       "v1--combobox-target": "hidden",
                     } %>

  <%= render Viral::BaseComponent.new(**combobox_arguments) %>

  <%= tag.div(
    id: @listbox_id,
    role: "listbox",
    "aria-label": combobox_arguments[:aria][:label],
    "data-v1--combobox-target": "listbox",
  ) do %>
    <%= @listbox_options %>
  <% end %>

  <div class="sr-only" aria-live="polite" data-v1--combobox-target="ariaLiveUpdate"></div>
<% end %>
