<div class="flex items-center" data-controller="clipboard">
  <span>
    <% if content.present? %>
      <%= content %>
    <% else %>
      <%= value %>
    <% end %>
  </span>
  <%= p_button(
    scheme: :unstyled,
    data: { action: 'click->clipboard#copy' },
    'data-clipboard-target': 'button',
    class: @button_classes,
    title: t('components.clipboard.title'),
    'aria-label': t('components.clipboard.title'),
    value: value
  ) do |c| %>
    <% c.with_leading_visual_icon(icon: :clipboard) %>
  <% end %>
  <div
    data-clipboard-target="content"
    role="tooltip"
    class="
      absolute z-10 invisible inline-block px-3 py-2 text-sm font-medium text-white
      transition-opacity bg-green-800 rounded-lg shadow-sm opacity-80 duration-600
      tooltip dark:bg-green-700
    "
  >
    <%= t("components.clipboard.copied") %>
  </div>
</div>
