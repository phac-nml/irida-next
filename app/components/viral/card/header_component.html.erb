<%= render Viral::BaseComponent.new(tag: 'div', **system_arguments) do %>
  <% if title_only? %>
    <h3 class="text-lg font-semibold text-slate-800 dark:text-white">
      <%= title %>
    </h3>
    <% if subtitle.present? %>
      <p class="text-slate-500 dark:text-slate-400">
        <%= subtitle %>
      </p>
    <% end %>
  <% else %>
      <% if actions.present? %>
        <div class="flex">
          <h3 class="flex-1 text-lg font-semibold text-slate-800 dark:text-white">
            <%= title %>
            <p class="text-slate-500 dark:text-slate-400">
              <%= subtitle %>
            </p>
          </h3>
          <div>
            <% actions.each do |action| %>
              <span class="ml-2"><%= action %></span>
            <% end %>
          </div>
        </div>
      <% else %>
      <h3 class="flex-1 text-lg font-semibold text-slate-800 dark:text-white">
          <%= title %>
      </h3>
      <p class="text-slate-500 dark:text-slate-400">
        <%= subtitle %>
      </p>
      <% end %>
  <% end %>
  <p class="text-slate-500 dark:text-slate-400"><%= content %></p>
<% end %>
