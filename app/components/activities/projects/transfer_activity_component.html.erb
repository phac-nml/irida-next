<span>
  <%= t(
    "#{@activity[:key]}",
    user: @activity[:user],
    old_namespace: link_to(
          @activity[:old_namespace],
          "#",
          class: "text-slate-800 dark:text-slate-300 font-medium cursor-not-allowed",
          disabled: true
        ),
        disabled: true,
    new_namespace: link_to(
          @activity[:new_namespace],
          "#",
          class: "text-slate-800 dark:text-slate-300 font-medium cursor-not-allowed",
          disabled: true
        )
  ) %>
</span>
