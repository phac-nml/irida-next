<span>
  <%= t(
    "#{@activity[:key]}",
    user: @activity[:user],
    old_namespace: @activity[:old_namespace],
    new_namespace: @activity[:new_namespace],
    transferred_group_puid: @activity[:transferred_group_puid],
  ) %>
</span>
